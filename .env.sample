# === バージョン指定 ===
WP_VERSION=6.7
PHP_VERSION=8.3

# === ポート ===
WP_PORT=8888
PMA_PORT=8080

# === サブディレクトリ構成 ===
# WPコアを /app に閉じ込める（空文字ならルート直下のまま）
WP_SUBDIR=app

# === ホスト側のdistディレクトリ ===
# WP_SUBDIRとは独立して設定できる（サブディレクトリなしでも空にならない）
DIST_PATH=dist/app
